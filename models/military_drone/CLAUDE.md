# military_drone

Alternative drone visual model used for variety in testing. Not flown — used as a scene prop.

## Files
- `model.sdf` — Visual-only SDF wrapping the STL mesh.
- `model.config` — Gazebo model manifest.

## Asset directories
- `meshes/` — `military_drone.stl` visual mesh (~7 MB, pure asset).
