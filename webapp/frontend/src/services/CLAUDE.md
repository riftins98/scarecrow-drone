# services

Frontend API client layer.

## Files
- `api.ts` — Axios wrapper with typed helpers for the backend routes (sim lifecycle, flights CRUD, drone control, detection config). Base URL defaults to `http://localhost:8000`.
