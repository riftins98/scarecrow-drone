# scripts

Flight control scripts and shell launch utilities.

## Subdirectories
- `flight/` — Python flight mission scripts (see `flight/CLAUDE.md`)
- `shell/` — Bash scripts for simulation launch and environment setup (see `shell/CLAUDE.md`)
