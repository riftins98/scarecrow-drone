# navigation (unit tests)

Tests for `scarecrow/navigation/`.

## Files
- `__init__.py` — Package marker (empty).
- `test_navigation_unit.py` — `NavigationUnit` async facade: wall_follow loop, rotate delegation, stabilize delegation, circuit composition.
- `test_map_unit.py` — `MapUnit` recording + axis-aligned bounding box computation on finish_mapping.
