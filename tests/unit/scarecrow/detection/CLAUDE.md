# detection (unit tests)

Tests for `scarecrow/detection/`.

## Files
- `__init__.py` — Package marker (empty).
- `test_yolo_detector.py` — `YoloDetector` rate limiting, on_detection callback, preload_async with mocked Ultralytics YOLO.
